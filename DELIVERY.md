# EasePod 0.1.0

Android 14（API 34）及以上。当前版本覆盖本地播放器、通用插件平台和可选安装的网易云音乐插件。界面与轮盘规则采用已经冻结的设计，实际测试范围见源码内的验证记录。

## 文件

| 文件 | 用途 |
|---|---|
| `easepod-0.1.0-debug.apk` | 已签名的开发体验包，可直接安装 |
| `easepod-0.1.0-release-unsigned.apk` | R8 优化后的未签名构建，发行者签名后方可安装 |
| `easepod-sample-plugin-0.1.0-debug.apk` | 独立的 SoundHelix 示例服务，可选安装，不随宿主预装 |
| `easepod-netease-plugin-0.1.0-debug.apk` | 独立的网易云音乐服务插件，与宿主一起分发，可选择是否安装 |
| `easepod-0.1.0-source.tar.gz` | 与本批 APK 一起生成的源码及构建材料 |
| `SHA256SUMS` | 文件 SHA-256 校验值 |
| `LICENSE` | AGPL-3.0-only 全文 |

运行 `sha256sum -c SHA256SUMS` 核对完整性。解包源码后阅读 `README.md` 与 `verification/README.md`，使用 JDK 17、Android SDK 35 和 Gradle Wrapper 8.9 构建。源码不包含 SDK、依赖缓存、本机配置或签名私钥，首次构建需要联网获取工具和依赖。

## 使用

安装主 APK 后，通过“设置 > 本地与存储 > 本地音乐文件夹”授予一个文件夹。根目录音频作为单曲，一级子目录作为专辑，不继续递归。应用首次运行不会自动添加曲目或播放音频。

示例插件需独立安装，在插件应用内批准 EasePod 宿主签名，再在 EasePod 插件管理中核对证书、信任并启用。它使用公共 HTTPS 音频，只演示实际声明的能力；在手机上可从插件详情选择“卸载插件”，也可从系统设置卸载，卸载后插件记录和账号会清理，并可重新安装交付包中的 APK。主题包通过本地安装入口导入，格式见源码内 `plugins/THEME-PACKAGE.md`。

网易云音乐以独立 APK 插件随交付包分发，用户可选择是否安装。安装主 APK 后，再按需安装 `easepod-netease-plugin-0.1.0-debug.apk`；插件会按照通用音乐插件契约出现在插件管理中，需要像其他外部插件一样核对签名并启用。未安装插件时，EasePod 保留本地音乐和其他已安装服务，启动应用不会自动发起网易云请求。账号页提供扫码登录，支持目录、搜索、播放、歌词和账号喜欢状态；歌曲权益与实际音质由服务端决定。账号凭据保存在本机 Android Keystore 加密文件中，不进入备份。插件可在插件详情或系统设置中停用、启用和卸载，卸载后宿主会清理该插件的连接、账号和来源选择；重新安装同一 APK 后可再次批准并启用。当前不声明离线下载和云端歌单编辑能力。

网易云插件没有桌面启动图标；宿主通过显式授权 Intent 打开插件授权页，通过 `MUSIC_PLUGIN` 服务 Intent 连接服务。

登录成功后，正常音乐菜单的 Cover Flow、歌单、歌曲、收藏专辑、艺人、搜索、每日推荐及主菜单随机播放直接使用该云账号。“音乐 > 音乐来源”可切回本地或其他已选服务，选择随重启保留；退出账号、失效、停用服务或安全模式会回到本地。后续服务复用通用 `catalog.home` 契约，旧插件兼容原浏览根目录。网易云 Cover Flow 使用账号歌单封面，歌曲使用喜欢列表；个人流派目录暂不支持。

HTTP Cookie 修正后，真实扫码与账号复验、私有歌单、每日推荐和喜欢状态只读测试已通过；公共目录、搜索、播放地址、歌词及 Media3 播放和暂停也有真机证据。主音乐来源测试及覆盖边界见源码中的 `verification/music-source.md`，历史扫码失败和后续修正记录见 `verification/netease.md`。真实云端写入、全部歌曲权益和各档音质不因这些只读结果而自动视为已验收。

防误触、锁屏覆盖、全屏显示默认关闭。锁屏覆盖遵守 Android 窗口和后台启动限制，底部上滑直接请求系统 PIN 等解锁认证，敏感操作需要系统解锁后再次确认。全屏显示隐藏顶部系统状态栏，保留底部导航和播放器 LCD 状态栏，设置自动保存。设备覆盖范围、执行结果和尚未实测的硬件条件以源码中的验证记录为准。

调试签名用于开发体验，不能作为正式发行签名。对外发行时需提供真实签名及对应源码获取方式；本目录不代表已发布到应用商店或配置了在线插件目录。
