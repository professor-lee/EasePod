# EasePod 开发进度

更新：2026-09-09。正式工程位于 `project/`。登录后的云账号已成为主音乐来源；Cover Flow、歌单、歌曲、专辑、艺人和搜索直接读取当前账号。网易云的每日推荐、私人雷达、歌曲漫游作为 Cover Flow 与歌单中的只读虚拟歌单，不再占用独立主菜单入口。证据与版本边界见 [网易云集成验收](verification/netease.md) 与 [主音乐来源验收](verification/music-source.md)。

## 已冻结基线

用户于 2026-09-08 确认现有界面与交互约束。基线见 [冻结记录](../docs/easepod-architecture/frozen-ui-baseline.md)。不得自行改动菜单结构、轮盘语义、防误触、锁屏解锁门控与 Cover Flow 动画设计。网页评审工具不进入 Android 应用。

## 迭代状态

| 阶段 | 范围 | 状态 | 验收证据 |
|---|---|---|---|
| I0 | 基线冻结、工程结构、工具链 | 完成 | JDK 17 / SDK 35 / Gradle 8.9 Wrapper，独立示例 APK 已构建安装 |
| I1 | Room、本地 SAF 扫描、Proto DataStore、歌单与恢复 | 验证中 | 数据单测 46/46，本地库真机 7/7；私有文件加密备份往返通过，系统选择器全流程仍单独验收 |
| I2 | Media3 服务、队列、后台播放、缓存、睡眠定时 | 验证中 | 单测 10/10，播放与缓存真机 14/14，下载状态竞态修正后 13/13；公共 HTTPS 播放 1/1 |
| I3 | 固定页面、轮盘、Cover Flow、输入与锁屏交互 | 验证中 | DeviceShell 7/7、页面恢复 4/4、锁屏手势 9/9、窗口设置 2/2；真机上滑进入系统 PIN 页及取消返回通过 |
| I4 | AIDL 插件平台、主题包、安装管理、离线与备份 | 验证中 | 内置网易云增补前置批次插件单测 52/52；历史跨进程 Binder 12/12、主题/备份/会话集成 4/4，HTTP Cookie 修正后结果待登记 |
| I5 | 整体集成、故障恢复、设备验收、APK 交付 | 验证中 | 锁屏增补已交付；网易云旧批次 R8 Release/Lint 任务通过，模块有 1 项协议加密警告；HTTP Cookie 修正后的构建与本批交付仍待完成 |

## 约束与记录

- Android 14/API 34 为最低运行版本；Kotlin + Compose + Media3 + Room + Proto DataStore。
- 不复制参考项目许可不兼容的位图，正式项目采用自有资源与可分发图标。
- 真实云插件能力通过标准契约接入；无云插件时保留完整本地播放器。
- 测试、构建与设备结果在实际完成后登记；性能目标不冒充实测。
- 构建环境为 JDK 17、Android SDK 35、Gradle 8.9；Gradle 分发包已核对官方 SHA-256。
- 已连接 Android 14/API 34 设备（824 × 1648），尚未完成整体验收。

## 工作日志

### 2026-09-08

- 建立正式工程模块与纯 Kotlin 领域契约。
- 冻结现有设计稿和交互规格，开始分模块实现。
- 已接入 Room / Proto DataStore / SAF、Media3、AIDL、主题包和 Compose 主界面的初始实现。首次联网编译已完成依赖解析；正在修复编译及集成问题，暂不标记功能完成。
- 数据审查发现候选根目录提前写入、扫描取消与发布竞争、Provider 部分读取错误等问题，已进入修复与测试。
- 播放模块继续验证队列、受限 HTTPS 解析、缓存与下载；插件模块继续完成独立示例 APK 和双向宿主信任校验。
- 数据测试 21 项通过后，补充扫描发布时的事务内授权/锁屏检查、候选授权回收及导入歌单显式关联本地文件；新增测试正在统一执行。
- 真机验证示例插件 12/12 通过（8.258 秒）：跨进程握手、分页与查询绑定、取消与旧代次隔离、并发上限、插件进程死亡、立即撤销宿主信任、解绑后连接失效。
- 修复主应用菜单构建与 Compose 作用域编译问题；补充按授权显示离线命令、按下载状态显示操作、来源封面联网门控、许可证全文入口与系统文件选择结果归属检查。
- 播放进度改用真实 canSeek；触摸拖动先预览，释放提交，取消/离窗撤回。音质显示实际来源值；下载任务保存请求标识，避免旧 Worker 覆盖新任务。
- 主应用首次统一编译成功并安装到 HiBreak。播放真机 8/8 通过（5.845 秒），下载协调器 7/7 通过（2.150 秒）。下载测试使用真实 Room/WorkManager 与可控 DataSource，不代表公共 HTTPS 下载已验收。
- 通过真实系统目录选择器授权独立 `EasePod-QA` 静音夹具，扫描得到 3 首歌曲、2 张一级目录专辑；PNG/JPEG 封面成功显示，截图在 `verification/device/`。
- 补新建歌单并追加曲目的单事务写入、网络音质偏好、脱敏诊断预览/导出、参考致谢、安装返回签名/版本核验及导出期间配置恢复。
- 轮盘灵敏度在手势开始时冻结；补 Cover Flow 反射、外部主题字体/间距/位图槽位，默认主题数值不变。
- 新增 MP3/FLAC/OGG 内嵌歌词读取与 5 项本地库真机流程测试，正在回归；仍不把未执行的项目记为完成。
- 核心 2、数据 41、播放 8、插件 21、应用 3 项单测通过。本地库真机 5/5（17.265 秒）、Compose 6/6（9.821 秒）通过，生成 43 张截图。
- 设备原系统动画比例为 0；临时设为 1 验证 Cover Flow 双向中间帧，单例通过（2.253 秒），随后恢复为 0。截图视检和像素差确认实际封面移动，不能将原关闭动画批次当成动画验收。
- 补齐 IME 收起门控、远端列表加载后焦点修正、导出配置重建桥接、持久化安装收据，以及连续两次确认启动崩溃后的安全模式。新增回归仍在执行。
- 统一 Release 构建曾在 R8 阶段被终止，未生成 Release APK；Lint 所需许可证资产任务依赖已修正，分批重建和回归中。

### 2026-09-08 锁屏与全屏增补

- 按用户本次要求新增“全屏显示”，默认关闭，隐藏顶部系统栏，持久保存并兼容旧 Proto 与旧备份。窗口重建和重新获取焦点后重新应用。
- 锁屏覆盖下底部上滑直接请求系统 Keyguard 认证，防误触开启时可用；短滑、取消、横向、多指和失焦不触发，不续接待确认的敏感操作。增补已记入冻结基线。
- 修复已有云歌单结果跨模块可空字段的编译错误，补齐示例服务设备测试的 Media3 注解依赖。界面测试结束恢复原设置。
- 单测 125/125 通过：core 2、data 46、playback 10、plugins 47、app 20。主 APK、测试 APK、Debug Lint 构建通过，Lint 报告为 `No issues found.`。
- 应用设备回归去重共 33 项通过：手势 9、窗口 2、DeviceShell 7、页面恢复 4、集成 4、本地库 7。首轮全屏测试因 ROM 系统截图返回 null 失败，改用现有 Compose 截图后窗口及本地库 9/9 通过；ADB 全屏前后截图已视检。
- 播放与缓存 14/14、插件 Binder 12/12 通过。下载首轮 12 项中 1 项预期 COMPLETE 实际 FAILED，单项重跑 1/1、全量重跑 12/12；旧数据库关闭崩溃未再出现，但状态非稳定失败尚未闭环，不以一次绿色重跑宣称修复。
- 公共 HTTPS 示例服务 1/1 通过（56.524 秒），覆盖播放、拖动进度、停用及重新启用不自动恢复播放；应用设备回归去重共 34 项。R8 Release 构建通过（10 分 17 秒）。
- HiBreak 真机锁屏覆盖和防误触同时开启时，底部上滑直接显示系统 PIN 输入框，取消后回到仍锁定的播放器；未输入 PIN。锁屏覆盖、全屏显示和防误触均已恢复测试前状态。
- 检查下载失败发现迟到的 WorkManager 排队快照可覆盖 DOWNLOADING，导致传输取消。已补当前状态复核及受控迟到事件回归，单项 1/1（0.291 秒）、下载全量 13/13（4.799 秒）通过；首轮日志本身不足以证明失败只由这一处竞态造成。
- 包含下载修正的最终 Debug、Debug 测试 APK、Debug Lint 与 R8 Release 构建通过（3 分 11 秒）。Debug 签名校验通过并已更新安装；本轮单测 125 项和分批设备回归去重 73 项通过，完整硬件与故障恢复验收仍继续。
- 本批详细命令、日志与固定单测 XML 见 [窗口设置验收记录](verification/window-settings.md)。

### 2026-09-08 内置网易云增补

- 固定单测快照 130/130 通过：网易云 58、plugins 52、app 20，均无失败、错误或跳过；这些是 HTTP Cookie 修正前的批次，不与锁屏增补数量累加。
- 真机公共测试 2/2（7.615 秒）通过，覆盖服务 UID/非导出约束，以及手机网络推荐歌单、歌曲搜索、播放地址、分页歌词接口、Media3 静音播放与暂停。
- 账号 UI 最终 6/6（10.588 秒）、选择账号存储 5/5、真实 Keystore 4/4 通过。账号 UI 使用受控账号来源；二维码布局覆盖正常字号、大字号与全屏，截图解码通过。早期账号 UI 4 项包含在最终 6 项中，不重复计数。
- 二维码裁切调整后的 Debug 主 APK 与测试 APK 构建通过，真实二维码截图可解码。另一手机网易云 App 扫码提示“设备环境异常，禁止登录”，真实账号登录尚未通过，根因未定；同手机切换应用会取消会话是当前生命周期限制，不是本次失败原因。
- HTTP Cookie 修正前的 R8 Release 和 Debug Lint 任务通过（12 分 33 秒）。应用 Lint 无问题，网易云模块为 0 errors、1 warning（协议 AES/ECB）；这些结果不代表修改后的代码已通过构建、验收或交付。
- 固定报告、设备日志、复现入口和当前边界见 [网易云集成验收记录](verification/netease.md)。

## 当前验收队列

0. 完成内置网易云主音乐来源的最终真机回归、安装和交付包更新；真实云端写入、完整播放权益和所有质量档位仍需单独验收。

1. 验证真实网络断开/恢复下的下载重排队、外部服务下载/续传及前台通知，继续观察下载状态稳定性。
2. 补成功系统认证后的完整流程、系统文件选择器备份全流程、连续崩溃安全模式与进程终止恢复验收；Cover Flow 系统动画启用后的历史证据继续保留。
3. 按实际硬件覆盖蓝牙、耳机移除、音频焦点、电池限制和不同 ROM 锁屏行为；正式发行前配置发行签名与对应源码获取方式。

尚未执行或未通过的项目不能作为已完成功能的验收证据；蓝牙硬件和不同 ROM 的锁屏行为需按实际设备覆盖记录。
